trigger:
  branches:
    exclude:
      - "*"
  tags:
    include:
      - v*

pool:
  vmImage: Ubuntu 16.04

steps:
  - task: Npm@1
    inputs:
      commands: publish
      publishRegistry: useExternalRegistry
      publishEndpoint: npmjs
